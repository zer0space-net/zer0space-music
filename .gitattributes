# The image is built and run on Linux — keep LF in the working tree on every
# platform so nothing ends up with CRLF inside the container.
* text=auto eol=lf

# Binary / generated
*.png binary
*.jpg binary
*.webp binary
*.woff2 binary
*.woff binary
*.ttf binary
